{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Run Script: start",
            "request": "launch",
            "command": "pnpm run start",
            "cwd": "${workspaceFolder}"
        }
    ]
}
